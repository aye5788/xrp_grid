# Generated artifacts — never commit run outputs or snapshots.
last_report.json
proposals.json
observer_snapshot.db*
nightly.log
# Local config — the repo is public; the droplet address is not.
droplet.env
