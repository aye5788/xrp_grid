.env
*.db
*.log
__pycache__/
*.pyc
venv/
.venv/
HALT
magi.log

# Env files and backups — never commit (added 2026-05-16 after push protection block)
.env
.env.*
*.bak
*.bak.*

# Working clutter — never commit (added 2026-06-10)
scratch/
# Rollback snapshots stay on disk (CLAUDE.md references these paths) but are
# untracked so the public repo / DeepWiki only sees the current system.
archive/
.claude/
xrp_grid.cc.json
xrp_grid.cc.json.gz
CONFIRM_LIVE.disarmed.*
optimize/ireul/ireul.db
optimize/ireul/results_*.json
